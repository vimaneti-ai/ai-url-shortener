# Performance test report

- Date (UTC):
- Git commit:
- Target environment:
- Host specification:
- k6 version/image: `grafana/k6:0.54.0`
- Profile: `load` (900 redirect VUs + 100 analytics VUs)
- Analytics recording: enabled / disabled
- Command:

## Result

- Overall: PASS / FAIL
- Completed iterations:
- Failed checks:
- Redirect duration: avg / p95 / p99 / max
- Analytics duration: avg / p95 / p99 / max
- Redirect failure rate:
- Analytics failure rate:
- CPU/memory observations:

## Interpretation

State what the run demonstrates, the first observed bottleneck, and any limitations. Attach or link
the generated `performance/results/summary.json` when the report is submitted.
